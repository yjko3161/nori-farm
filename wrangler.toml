name = "nori-farm"
main = "worker/api-worker.js"
compatibility_date = "2025-01-01"
account_id = "e48253acb1080f83226d97a80edb263e"

# 정적 자산 (HTML, CSS, JS, 이미지 등) — repo 루트 전체, .assetsignore 로 제외 관리
[assets]
directory = "./"
binding = "ASSETS"

# D1 데이터베이스 바인딩
[[d1_databases]]
binding = "DB"
database_name = "nori-farm-db"
database_id = "f4c15443-9b0f-4c85-855b-86602692f543"

# Custom Domain (www.nori-farm.com 전체) 으로 라우팅됨 — routes 별도 명시 불필요
